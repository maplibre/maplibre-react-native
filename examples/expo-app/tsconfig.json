{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },

  // Required for local plugin resolution in app.config.ts
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}
